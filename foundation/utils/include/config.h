#include <stdio.h>
#include <nlohmann/json.hpp>
#include <spdlog/spdlog.h>

namespace ti {
namespace base {

class IConfig {

public:
    IConfig(/* args */);
    ~IConfig();

private:
    /* data */
};

} // namespace base
} // namespace ti